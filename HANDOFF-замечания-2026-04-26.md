# Handoff: исправления по замечаниям к расчёту вентиляции

**Дата:** 2026-04-26
**Источник замечаний:** `D:\РАБОТА ЛЮКСАР\myProgects\aero\extracted\Замечания.docx` (28 пунктов) + 11 проверочных схем `П.*.vnet`
**Анализ:** см. отчёт в чате (не сохранён в файл)

---

## Контекст проекта

- Это **frontend-only** приложение (HTML/CSS/Vanilla JS + Fabric.js).
- Точка входа — `index.html`. Сборки нет, открывается напрямую.
- Архитектура — IIFE-модули в `js-for-index4/`, экспорт в `window`. Порядок загрузки в `index.html` важен.
- Полное описание архитектуры — в [CLAUDE.md](CLAUDE.md).
- Расчётный движок [airSolver.js](js-for-index4/airSolver.js) **ЗАЩИЩЁН** — пользователь подтвердил, что BFS-ориентация работает верно. Не трогать без явного запроса.

### Запуск превью

В `D:\РАБОТА ЛЮКСАР\myProgects\aero\.claude\launch.json` уже создан конфиг:
```json
{
  "name": "aero-static",
  "runtimeExecutable": "python",
  "runtimeArgs": ["-m", "http.server", "8765", "--directory", "endless-_canvas_aero-main"],
  "port": 8765
}
```
Запуск: `mcp__Claude_Preview__preview_start` с `name: "aero-static"`. URL: `http://localhost:8765/index.html`.

---

## ✅ Сделано (15 пунктов)

| # | Замечание | Файл / строка | Что сделано |
|---|-----------|----------|--------|
| 1 | «Непрерывный» по умолчанию | `js-for-index4/main5.js:7`, `:94-96` | `isContinuousLineMode = true` + класс `active` |
| 2 | Подсветка активного режима рисования | `js-for-index4/canvasSetup.js:583-625` | `activateLineDrawing` ставит `active` на `#lineDrawingBtn`, `deactivateAllModes` снимает |
| 7+14 | Drag точки → разлёт схемы | `js-for-index4/canvasSetup.js:766-790` | Защита `_dragOrigX === undefined` — берём текущую позицию как origin |
| 8 | Undo/Redo не сбрасывает расчёт | `js-for-index4/undoRedo.js:15-30` | `_resetVisualStateAfterUndoRedo()` чистит smoke/flow/dangling/расчёт |
| 9 | Toggle для «Проверки связей» | `js-for-index4/visualization.js:487-499`, `:557` | Повторный клик снимает подсветку, кнопка получает `active` |
| 12 | Заблокировать вращение вентилятора | `js-for-index4/imageManager.js:317-323` | `lockRotation: true` на всех новых изображениях |
| **13** | **Пустая модалка точки разделения (КРИТ)** | `index.html:767` | `id="intersectionPointInfo"` → `id="intersectionPointModalContent"` |
| 15 | Сетка слетает после drag | `js-for-index4/canvasSetup.js:849` | `drawGrid()` после drag intersection-point |
| 18 | Авто-пересчёт при смене fanMode | `js-for-index4/modals.js:540-562` | `calculateAirFlowsSafe()` если изменился `fanMode`/`isFlowSource`/`Q0` |
| 19 | Ручной ввод масштаба | `index.html:151-168`, `styles/layout.css:158-172`, `js-for-index4/canvasSetup.js:15-22`, `:891` | `<input type="number">` + change-handler + экспорт `zoomToPoint` |
| 20 | «Вписать в экран» обрезает тексты | `js-for-index4/canvasSetup.js:37-78` | Тексты воздуха входят в bbox; padding 60→120 |
| 22 | Стрелки направления | `js-for-index4/visualization.js:120-130` | Оставлены только →/← (↓/↑ ломаются после поворота текста) |
| 23 | Точка пересечения по центру | `js-for-index4/intersections.js:97-106` | `originX/Y='center'` + сопутствующий код |
| 24 | Заблокировать масштаб/поворот точки | `js-for-index4/intersections.js:101` | `lockScalingX/Y: true, lockRotation: true` |
| 25 | Авто-пересчёт после splitAllLines | `js-for-index4/intersections.js:294-298` | `calculateAirFlowsSafe()` через 60ms после split |

### Проверено через preview (`http://localhost:8765/index.html`)
- Загрузка без ошибок в консоли ✓
- «Непрерывный» подсвечен по умолчанию ✓
- Кнопка «Ветвь» получает `active`/теряет ✓
- Точки строго в координатах пересечения ✓
- Toggle проверки связей работает (4 маркера → 0) ✓
- «Вписать в экран» масштабирует корректно ✓
- Стрелка `→` правильно ориентирована вдоль линии ✓

---

## ✅ Сделано в сессии 2026-04-26 (продолжение)

| # | Замечание | Где правлено |
|---|-----------|--------------|
| 28 / п.26-docx | Разделение по всем видимым слоям | `intersections.js:274-360` |
| 21 | Одиночный режим не закрывается после рисования | `canvasSetup.js:490-505` |
| 3 | Кнопка «Упростить» (склейка сегментов) | `chainAnalysis.js:201-394` + `index.html:86` |
| 4 | Маркеры на всех узлах ≥1 степени (синие endpoints, красные перекрёстки) | `intersections.js:316-345` |
| 5 | Запечатать/открыть тупик из модалки точки | `modals.js:36-77` |
| 6 | Вертикальная раскладка модалки параметров линии | `modal.css:152-172` + `index.html:393` |
| 10 | Объект привязывается только к линии (≤35 px) | `imageManager.js:302-320` |
| 11 | Жёлтая подсветка линии/узла привязки выделенного объекта | `visualization.js:285-353` + `canvasSetup.js:243-256` |
| 27 | Tooltip-onboarding для cross-layer + проверен граф | `canvasSetup.js:653-710` + `index.html:68` |
| **26** | «Струя» включена по умолчанию + UI sync | `visualization.js:308` + `main5.js:98-100` |
| **17** | Запрет отрицательных Q + Math.abs защита | `index.html:610` + `modals.js:539-545` + `airModel.js:80` |

## ⏳ Осталось сделать

### КРИТИЧНЫЕ (требуют осторожности — связаны с защищённым `airSolver.js`)

| # | Замечание | Файл | Сложность | Что нужно |
|---|-----------|------|-----------|-----------|
| 16 | 2 реверс-вентилятора ломают расчёт | `airSolver.js` | высокая | См. отдельный хендофф [HANDOFF-п16-2reverse-fans.md](HANDOFF-п16-2reverse-fans.md). Зона: `:486-571` (ориентация рёбер). Тест: `extracted/П.16.vnet`. |

---

## Важные ограничения

1. **Защищённый солвер** ([airSolver.js](js-for-index4/airSolver.js)) — BFS-ориентация и Гаусс-Зейдель не модифицировать.
2. **`roundTo5()`** — округление до 5 знаков, только для финальных значений.
3. **`getPointKey()`** ([utils.js](js-for-index4/utils.js)) — округление до 2 знаков для ключей узлов (компенсация микро-расхождений Fabric.js).
4. **`splitLineAtPoint()`** ([intersections.js:301](js-for-index4/intersections.js#L301)) — порог 10px на «огрызки».
5. После любого изменения `originX/Y` точки — обновить **все** места, где `obj.left + radius`/`obj.top + radius` используется как центр.

---

## Файлы для тестирования

В `D:\РАБОТА ЛЮКСАР\myProgects\aero\extracted\`:
- `П.8.vnet` — без вентилятора, чистая сеть (6 линий)
- `П.11.vnet` — 1 fan supply (7 линий)
- `П.12.vnet`, `П.13.vnet`, `П.14.vnet` — 1 fan reverse
- `П.15.vnet` — без вентилятора
- `П.16.vnet` — **2 fan reverse** (тест п.16)
- `П.20.vnet` — минимальная сеть (3 линии)
- `П.21.vnet` — 1 fan supply, расчёт корректный
- `П.24.vnet` — 21 линия, 2 fan reverse, сложная сеть
- `П.25.vnet` — 27 линий, 3 fan supply, **2 слоя** (тест п.27-28)

Формат `.vnet`: `AERONET1` (8 байт magic) + `\x01` (версия) + base64(JSON). Распаковка — см. [projectManager.js:22](js-for-index4/projectManager.js#L22) (`unpackFile`).

---

## Полезные команды для следующей сессии

```bash
# Распаковать .vnet:
python -c "
import base64, json
with open('extracted/П.21.vnet', 'rb') as f: data = f.read()
print(json.dumps(json.loads(base64.b64decode(data[9:])), indent=2, ensure_ascii=False))
"

# Запустить превью:
# mcp__Claude_Preview__preview_start (name: aero-static)
# URL: http://localhost:8765/index.html
```

---

## Ссылки

- [CLAUDE.md](CLAUDE.md) — архитектура проекта (создан до этой сессии)
- [ЗАМЕЧАНИЯ-СТАТУС.md](ЗАМЕЧАНИЯ-СТАТУС.md) — старый статус замечаний из «Замечания 12-04.docx» (другая итерация)
- Свежий docx с замечаниями: `D:\РАБОТА ЛЮКСАР\myProgects\aero\extracted\Замечания.docx`
